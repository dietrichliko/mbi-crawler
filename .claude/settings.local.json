{
  "permissions": {
    "allow": [
      "Bash(chmod:*)",
      "Bash(mbi-crawler list-sites)",
      "Bash(uv run mbi-crawler list-sites)",
      "Bash(uv run mbi-crawler crawl --site mbi_oeaw -v)",
      "Bash(uv run python -c \":*)"
    ]
  }
}
